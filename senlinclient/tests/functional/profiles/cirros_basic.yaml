type: os.nova.server
version: 1.0
properties:
  flavor: 1
  image: "cirros-0.3.5-x86_64-disk.img"
  networks:
   - network: private
  metadata:
    test_key: test_value
  user_data: |
    #!/bin/sh
    echo 'hello, world' > /tmp/test_file
